<Window x:Class="HHDCTracker.Views.Children.AddAdjustmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Manual Adjustment" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F2F3F4">
    <StackPanel Margin="24">
        <TextBlock Text="Add Manual Adjustment" FontSize="16" FontWeight="Bold"
                   FontFamily="Segoe UI" Foreground="#1A5276" Margin="0,0,0,20"/>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Date" FontSize="11" Foreground="#888"
                           FontFamily="Segoe UI" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtDate" Padding="6,4" FontFamily="Segoe UI" FontSize="13"
                         BorderBrush="#2E86C1"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Amount ($)" FontSize="11" Foreground="#888"
                           FontFamily="Segoe UI" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtAmount" Padding="6,4" FontFamily="Segoe UI" FontSize="13"
                         BorderBrush="#2E86C1"/>
            </StackPanel>
        </Grid>

        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="Apply To" FontSize="11" Foreground="#888"
                       FontFamily="Segoe UI" Margin="0,0,0,4"/>
            <ComboBox x:Name="CboApplyTo" FontFamily="Segoe UI" FontSize="13" Height="34">
                <ComboBoxItem Content="Balance"/>
                <ComboBoxItem Content="Credit"/>
                <ComboBoxItem Content="Balance from Credit"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="Invoice # (required for Balance types)"
                       FontSize="11" Foreground="#888" FontFamily="Segoe UI" Margin="0,0,0,4"/>
            <ComboBox x:Name="CboInvoice" FontFamily="Segoe UI" FontSize="13"
                      Height="34" DisplayMemberPath="InvoiceNumber"/>
        </StackPanel>

        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Reason / Notes" FontSize="11" Foreground="#888"
                       FontFamily="Segoe UI" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtReason" Padding="6,4" FontFamily="Segoe UI" FontSize="13"
                     BorderBrush="#2E86C1" Height="60" AcceptsReturn="True" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,8" FontFamily="Segoe UI" FontSize="13"
                    Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Save Adjustment" Style="{StaticResource PrimaryButton}"
                    Padding="16,8" Click="Save_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
